{
  "name": "@festival/announcements-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Polkadot chat extension that posts festival announcements into the host chat as they're published on-chain. Ships only as the web3summit.dot worker modality — no standalone .dot.",
  "scripts": {
    "build": "tsx build.mts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@novasamatech/host-api": "0.8.2",
    "@novasamatech/host-api-wrapper": "0.8.2",
    "@parity/product-sdk-cloud-storage": "^0.5.5",
    "polkadot-api": "^2.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "tsx": "^4.21.0",
    "typescript": "^5.7.3"
  }
}
